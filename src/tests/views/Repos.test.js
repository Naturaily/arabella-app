test('fetchRepos - on succeess on call for repos, Promise.all is executed', () => {
    // mock axios
    // mock request
    // mock response
    // check if promise fires
});

test('fetchRepos - on fail on call for repos, error is set', () => {
    // mock axios
    // mock request
    // mock failure
    // check if fetchError is set to true
});

test('fetchRepos - Promise.all succeeds', () => {
    // mock axios
    // mock request
    // mock reponse
    // check if repos change from empty array
});

test('fetchRepos - Promise.all fails', () => {
    // mock axios
    // mock request
    // mock failure
    // check if fetchError is set to true
});

test('useEffect - fetchRepos is called on load', () => {
    // check if useEffect hook calls fetchRepos
});

test('toggleModal - if createVisible is false, it is set to true', () => {
    // get state
    // run function
    // check state changes
});

test('toggleModal - if createVisible is true, it is set to false', () => {
    // get state
    // run function
    // check state changes
});
