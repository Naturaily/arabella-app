test('SIGN_IN changes state appropriately', () => {
    // mock state
    // mock dispatch
    // compare state to predicted state
});

test('SIGN_IN_ERROR changes state appropriately', () => {
    // mock state
    // mock dispatch
    // compare state to predicted state
});

test('SIGN_OUT changes state appropriately', () => {
    // mock state
    // mock dispatch
    // compare state to predicted state
});

test('SIGN_OUT_ERROR changes state appropriately', () => {
    // mock state
    // mock dispatch
    // compare state to predicted state
});

test('inproper action doesn\'t change state', () => {
    // mock state
    // mock dispatch
    // compare state to predicted state
});
