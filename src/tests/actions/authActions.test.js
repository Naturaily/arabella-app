test('signIn dispatches SIGN_IN on success', () => {
});

test('signIn sets data in localstorage on success', () => {
});

test('signIn dispatches SIGN_IN_ERROR on failure', () => {
});

test('signOut dispatches SIGN_OUT on success', () => {
});

test('signOut removes data from localstorage on success', () => {
});

test('signOut dispatches SIGN_OUT_ERROR on failure', () => {
});
